{
  "name": "sword_game",
  "type": "module",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "concurrently \"tsc --watch\" \"cpx \\\"src/**/*.{html,css}\\\" dist --watch\"",
    "build": "tsc && cpx \"src/**/*.{html,css}\" dist",
    "start": "electron ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.0",
    "cpx": "^1.5.0",
    "electron": "^37.2.1",
    "typescript": "^5.8.3"
  }
}
